#!/usr/bin/env node

var info = require('../package.json');
var program = require('commander');
var generate = require('../lib/command/generate');

program
  .version(info.version)
;

generate(program);

program.parse(process.argv);